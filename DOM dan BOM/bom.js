/*
    prompt pada BOM
    inputan akan selalu string, merubah kedalam number sebagai berikut

*/

let pesanInputan = Number(prompt("Masukan nilai angkamu ..")); // hanya bisa di lakukan pada browser environment
let tipeNumber = parseInt(prompt("Masukan nilaimu.."));
// cara memasukan nilai default pada prompt

let nilaidefault = prompt("Masukan nilaimu..", "Ketikan huruf")
